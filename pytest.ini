[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --color=yes
    --no-cov
    -p multi_eden.pytest_plugin

markers =
    critical: Critical tests that stop the suite if they fail
